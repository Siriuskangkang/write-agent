# 详细改动清单 - 数据库与依赖

## 一、数据库改动

### 1.1 StyleTemplate 表
**文件：`backend/src/style-template/entities/style-template.entity.ts`**

**结论：无需修改**

现有字段已满足需求：
- `features` (json) - 存储体例特征，支持编辑
- `status` (enum) - 已有 pending/analyzing/completed/failed
- `error_message` (text) - 已有错误信息字段

### 1.2 其他表

**结论：无需修改**

所有改动都是业务逻辑层面，不涉及数据库结构变更。

---

## 二、依赖包改动

### 2.1 后端新增依赖

**文件：`backend/package.json`**

```bash
npm install openai
```

**版本：**
- `openai`: ^4.0.0（最新稳定版）

**用途：**
- DeepSeek Provider 使用 OpenAI SDK

### 2.2 前端依赖

**结论：无需新增**

所有功能使用现有依赖：
- Ant Design（已有）
- React（已有）
- Next.js（已有）

---

## 三、环境变量改动

### 3.1 后端环境变量

**文件：`backend/.env`**

**新增：**
```env
# LLM 提供商选择
LLM_PROVIDER=anthropic

# DeepSeek 配置（新增）
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
```

**保持不变：**
```env
ANTHROPIC_API_KEY=
ANTHROPIC_BASE_URL=
ANTHROPIC_MODEL=claude-sonnet-4-6
```

### 3.2 前端环境变量

**结论：无需修改**

---

## 四、配置文件改动

### 4.1 TypeScript 配置

**结论：无需修改**

### 4.2 ESLint/Prettier

**结论：无需修改**

### 4.3 PM2 配置

**文件：`ecosystem.config.cjs`**

**结论：无需修改**

服务启动方式不变。

---

## 五、迁移文件

### 5.1 数据库迁移

**结论：无需创建**

没有数据库结构变更，不需要迁移文件。

---

## 六、测试文件

### 6.1 需要新增的测试

**后端：**
- `backend/src/llm/llm.factory.spec.ts` - LLM 工厂测试
- `backend/src/llm/providers/anthropic.provider.spec.ts` - Anthropic Provider 测试
- `backend/src/llm/providers/deepseek.provider.spec.ts` - DeepSeek Provider 测试

**前端：**
- `frontend/src/components/workbench/StyleTemplate/StyleTemplateEdit.test.tsx` - 编辑组件测试

### 6.2 需要更新的测试

**后端：**
- `backend/src/agent/agent.service.spec.ts` - 更新 AgentService 测试
- `backend/src/content/content.service.spec.ts` - 更新 ContentService 测试

---

## 七、文档改动

### 7.1 README

**文件：`backend/README.md`**

需要补充：
- LLM_PROVIDER 环境变量说明
- DeepSeek 配置说明

### 7.2 API 文档

**结论：无需修改**

API 接口没有变化。
