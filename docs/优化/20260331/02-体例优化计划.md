# 体例优化计划

## 模块：体例功能完善

### 目标
1. 生成目录前强制检查体例
2. 将体例特征注入到生成 prompt 中
3. 添加体例编辑功能

---

## 一、前端：生成前检查体例

### 1.1 修改 ChatPanel 组件
**文件：`frontend/src/components/workbench/ChatPanel.tsx`**

**新增导入：**
```typescript
import { Modal } from 'antd';
import { useRouter } from 'next/navigation';
import { getProjectStyleTemplate } from '@/services/styleTemplateApi';
```

**修改生成目录方法：**
```typescript
const handleGenerateDirectory = async () => {
  // 检查体例
  const template = await getProjectStyleTemplate(projectId);

  if (!template || template.status !== 'completed' || !template.features) {
    Modal.confirm({
      title: '未设置体例',
      content: '生成目录前需要先上传体例文件进行分析，是否前往上传？',
      okText: '前往上传',
      cancelText: '取消',
      onOk: () => {
        router.push(`/projects/${projectId}/style-templates`);
      },
    });
    return;
  }

  // 继续原有的生成逻辑
  // ...
};
```

**同样修改：**
- `handleGenerateOutline` - 生成大纲前检查
- `handleGenerateContent` - 生成正文前检查

---

## 二、后端：体例服务增强

### 2.1 新增方法
**文件：`backend/src/style-template/style-template.service.ts`**

**新增：**
```typescript
async getProjectActiveTemplate(projectId: string): Promise<StyleTemplate | null> {
  return this.styleTemplateRepository.findOne({
    where: {
      projectId,
      status: 'completed',
    },
    order: {
      updatedAt: 'DESC',
    },
  });
}
```

---

## 三、后端：目录生成注入体例

### 3.1 修改 ContentService
**文件：`backend/src/content/content.service.ts`**

**在 initDirectorySse 方法中：**

```typescript
async *initDirectorySse(projectId: string, dto: GenerateDirectoryDto) {
  // 1. 检查体例
  const template = await this.styleTemplateService.getProjectActiveTemplate(projectId);
  
  if (!template || !template.features) {
    throw new BadRequestException('请先上传并分析体例文件');
  }

  // 2. 构建体例上下文
  const styleContext = this.buildStyleContext(template.features);

  // 3. 传递给 chain
  const context = {
    projectId,
    userInput: dto.userInput,
    styleContext,  // 新增
  };

  // ...
}

private buildStyleContext(features: any): string {
  return `
## 体例规范

请严格遵循以下体例规范：

### 结构规范
- 章节格式：${features.structure?.chapterFormat || '第X章'}
- 小节格式：${features.structure?.sectionFormat || 'X.X'}
- 最大层级：${features.structure?.maxDepth || 3}

### 语言风格
- 语气：${features.language?.tone || 'formal'}
- 人称：${features.language?.person || 'third'}
  `.trim();
}
```

---

## 四、后端：修改 Chain

### 4.1 修改 directory.chain.ts
**文件：`backend/src/agent/chains/directory.chain.ts`**

**修改接口：**
```typescript
export interface DirectoryChainInput {
  projectId: string;
  userInput: string;
  styleContext: string;  // 新增
}
```

**修改 prompt 构建：**
```typescript
const prompt = `
${input.styleContext}

## 用户需求
${input.userInput}

请根据上述体例规范生成目录结构...
`;
```

